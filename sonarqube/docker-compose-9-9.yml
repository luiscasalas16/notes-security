version: "3"

networks:
  sonarnet:

services:
  sonarqube:
    container_name: sonarqube_9_9
    image: sonarqube:9.9-community
    restart: unless-stopped
    depends_on:
      - postgresql
    expose:
      - 9000
    ports:
      - 9000:9000
    networks:
      - sonarnet
    environment:
      - SONAR_JDBC_URL=jdbc:postgresql://postgresql:5432/sonar
      - SONAR_JDBC_USERNAME=sonar
      - SONAR_JDBC_PASSWORD=sonar
    volumes:
      - sonarqube_data:/opt/sonarqube/data
      - sonarqube_extensions:/opt/sonarqube/extensions
      - sonarqube_logs:/opt/sonarqube/logs

  postgresql:
    container_name: postgresql_9_9
    image: postgres:12.19
    restart: unless-stopped
    networks:
      - sonarnet
    environment:
      - POSTGRES_USER=sonar
      - POSTGRES_PASSWORD=sonar
    volumes:
      - postgresql:/var/lib/postgresql
      - postgresql_data:/var/lib/postgresql/data

volumes:
  sonarqube_data:
    name: sonarqube_data_9_9
    driver: local
    driver_opts:
      o: bind
      type: none
      device: C:\sonar\9-9\sonarqube_data
  sonarqube_extensions:
    name: sonarqube_extensions_9_9
    driver: local
    driver_opts:
      o: bind
      type: none
      device: C:\sonar\9-9\sonarqube_extensions
  sonarqube_logs:
    name: sonarqube_logs_9_9
    driver: local
    driver_opts:
      o: bind
      type: none
      device: C:\sonar\9-9\sonarqube_logs
  postgresql:
    name: postgresql_9_9
    driver: local
    driver_opts:
      o: bind
      type: none
      device: C:\sonar\9-9\postgresql
  postgresql_data:
    name: postgresql_data_9_9
    driver: local
    driver_opts:
      o: bind
      type: none
      device: C:\sonar\9-9\postgresql_data